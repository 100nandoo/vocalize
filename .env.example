GEMINI_API_KEY=put_api_key_here
GROQ_API_KEY=put_api_key_here
SUMMARIZER_PROVIDER=groq
# DEFAULT_VOICE=Kore
# DEFAULT_MODEL=gemini-2.5-flash-preview-tts
# PORT=8080
# HOST=127.0.0.1

# Set this to protect all API endpoints with a master key.
# Use it to access /api-keys.html and bootstrap your first API key.
# Generate one with: openssl rand -hex 32
#               or:  python3 -c "import secrets; print(secrets.token_hex(32))"
# INTI_MASTER_KEY=change_me_to_a_strong_secret
